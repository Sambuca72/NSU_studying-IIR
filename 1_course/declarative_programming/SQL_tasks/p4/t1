SELECT WARE AS PRODUCT
FROM (SELECT WARE, AVG(PRICE) AS AVERAGE
		FROM PRODUCT 
		GROUP BY WARE) AVGS
WHERE AVERAGE = (SELECT MAX(AVERAGE) 
					FROM (SELECT AVG(PRICE) AS AVERAGE
						FROM PRODUCT 
						GROUP BY WARE)MAX_AVERAGE)
						