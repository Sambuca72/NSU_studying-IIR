SELECT DISTINCT COMPANY
FROM MANUFACTURER
WHERE COMPANY NOT IN
		(SELECT M.COMPANY 
			FROM PRODUCT P, (SELECT AVG(PRICE) AS AVG, WARE FROM PRODUCT GROUP BY WARE) PR
			JOIN MANUFACTURER M ON M.RECIPE_ID = P.RECIPE_ID
			WHERE P.PRICE <= AVG * 1.2
			AND PR.WARE = P.WARE)