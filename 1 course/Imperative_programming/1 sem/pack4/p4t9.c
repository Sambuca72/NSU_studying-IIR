#include <stdio.h>
#include <string.h>

int main(){
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    int frames;
    float tdt,pbt,mdt,adt,madt;
    char st[10000];
    st[0]="\0";
    gets(st)
    if 

    printf("Video \n  Total frames: %d ms\n  Total decode time:"  
    "%0.3f ms\n  Actual playback time:  %0.3f ms"
    "\n  Min/avg/max decode time: %0.3f/%0.3f/%0.3f ms\n",frames,tdt,pbt,mdt,adt,madt);

    fclose(stdin);
    fclose(stdout);
}