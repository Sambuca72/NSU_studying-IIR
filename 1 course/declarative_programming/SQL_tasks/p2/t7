SELECT DISTINCT PRODUCT.WARE
FROM PRODUCT
EXCEPT
SELECT DISTINCT PRODUCT.WARE
FROM PRODUCT, MATERIAL, CATEGORY 
WHERE PRODUCT.RECIPE_ID = MATERIAL.RECIPE_ID
AND MATERIAL.WARE = CATEGORY.WARE
AND CLASS <> 'Mineral'
ORDER BY PRODUCT.WARE ASC